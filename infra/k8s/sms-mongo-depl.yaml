apiVersion: apps/v1
kind: Deployment
metadata:
  name: sms-mongo-depl
spec:
  replicas: 1
  selector:
    matchLabels:
      app: sms-mongo
  template:
    metadata:
      labels:
        app: sms-mongo
    spec:
      containers:
        - name: sms-mongo
          image: mongo
---
apiVersion: v1
kind: Service
metadata:
  name: sms-mongo-srv
spec:
  selector:
    app: sms-mongo
  ports:
    - name: db
      protocol: TCP
      port: 27017
      targetPort: 27017
